# Dependencies for the Vercel Python function (api/index.py).
# Kept separate from the scraper's requirements.txt so the serverless
# bundle stays small -- the scraper is not deployed.
fastapi>=0.115.0
sqlalchemy>=2.0.36
psycopg[binary]>=3.2.0
pydantic>=2.9.0
# Optional at runtime (Vercel injects real env vars, and the import is
# guarded), but included so local and deployed behaviour match exactly.
python-dotenv>=1.0.1
