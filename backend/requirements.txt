# Local development dependencies.
#
# Version bounds mirror backend/pyproject.toml, which is the source of
# truth for the deployed function. The Starlette upper bound matters:
# Starlette 1.x changed router internals so `include_router` silently
# attaches nothing -- the app boots and serves /docs while every business
# endpoint disappears.
fastapi>=0.115.0,<0.116
starlette>=0.37,<0.42
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.36,<3
psycopg[binary]>=3.2.0,<4
pydantic>=2.9.0,<3
python-dotenv>=1.0.1,<2
httpx>=0.27.0
pytest>=8.3.0
